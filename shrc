#!/bin/sh

# koopa shell
# https://koopa.acidgenomics.com/
# bcbio configuration, based on hostname.
case "$(uname -n)" in
    azlabapp*)
        export BCBIO_EXE="/data00/bcbio/v1.1.5/tools/bin/bcbio_nextgen.py"
        ;;
esac
# shellcheck source=/dev/null
. "${HOME}/.config/koopa/activate"
