# EditorConfig
# https://editorconfig.org

# Top-most EditorConfig file.
root = true

# Unix-style newlines with a newline ending every file.
# Using spaces instead of tabs by default.
[*]
charset = utf-8
end_of_line = lf
indent_size = 4
indent_style = space
insert_final_newline = true
trim_trailing_whitespace = true
