# Ensure `dyn.load()` is configured correctly.
#
# Define lib paths in `/etc/ld.so.conf.d/local-x86_64.conf/`.
# Add multiple paths there using a line break.
#
# /usr/local/lib64
# /usr/local/lib
#
# If you install new software (e.g. gsl, hdf5) need to update config.
# Run `sudo ldconfig`.
#
# You can check the linkage using `ldd`.
# For example:
# ldd /usr/local/bin/emacs
#
# Alternatively, can export LD_LIBRARY_PATH in `.shrc`.
# Generally this is BAD and should be avoided.
# This must be done per user though.
#
# This step is needed to ensure hdf5r loads correctly.
#
# See also:
# - `/etc/ld.so.conf.d/` approach (recommended).
#   https://stackoverflow.com/a/45228086
#   https://stackoverflow.com/a/696247
# - https://stackoverflow.com/questions/45112506
# - https://stat.ethz.ch/R-manual/R-devel/library/base/html/getLoadedDLLs.html
#
# Avoid setting `LD_LIBRARY_PATH`!!!
# LD_LIBRARY_PATH="/usr/local/lib64:/usr/local/lib:/usr/lib64/R/lib:/usr/lib64:/usr/lib:/lib64:/lib"

# Restricting PATH so we don't load any binaries from conda.
PATH="/usr/local/bin:/usr/bin:/bin"

R_LIBS_USER="~/R/x86_64-redhat-linux-gnu-library/3.5/bioc-3.8-release"

RETICULATE_PYTHON="~/.conda/envs/reticulate/bin/python"

TZ="America/New_York"
